## 创建项目

### 初始化项目

使用pnpm+monorepo管理项目依赖，先初始化一个项目。

```bash
pnpm init
```

### 创建monorepo项目

想要使用monorepo管理项目需要创建一个文件叫`pnpm-workspace.yaml`，并且要告诉monorepo管理的目录是哪一个。
(新增)code\pnpm-workspace.yaml

```yaml
packages:

  - "packages/*"
```

### 当前目录

创建packages目录，创建完以后，当前项目目录就是这样了

```
├─📁 packages/-------------- # 包
├─📄 package.json----------- # 项目包
└─📄 pnpm-workspace.yaml---- # monorepo管理配置
```

### 下载TS

配置完monorepo之后，下载一个依赖(之前可能需要再后面加 -w 但是现在不需要了)

```bash
pnpm install typescript @types/node -D
```

### 初始化TS、配置TS

执行ts初始化（执行当前项目初始化，而不是全局，所以这里用的是npx）

```bash
npx tsc --init
```

配置ts
code\tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ESNext", // 指定 ECMAScript 目标版本
    "module": "ESNext", // 指定模块代码生成规范
    "moduleResolution": "node", // 指定模块解析策略
    "outDir": "dist", // 指定编译输出的目录
    "resolveJsonModule": true, // 允许导入 JSON 文件
    "strict": false, // 关闭严格模式
    "lib": ["ESNext", "DOM"], // 指定要使用的库文件
    "baseUrl": "./"
  }
}
```

### 下载esbuild

通过esbuild打包，先下载esbuild
处理打包配置，需要先装一个依赖，这里是用esbuild来打包，并且

```bash
pnpm install esbuild -D
```

### 配置dev命令

先配置了一个dev的命令，用来执行scripts下的dev文件
code\package.json

```json
{
  "name": "Vue3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "esbuild": "^0.27.5",
    "typescript": "^6.0.2"
  }
}
```

> 注意：这里加了一个type，因为项目是需要支持ES6模块化的，所以这里将type改成module，改成module后，项目就变成了ES模块化的项目了

### 下载、配置prettier

下载prettier，用来代码格式化

```bash
pnpm install prettier -D
```

配置
(新增)`code\.prettierrc`

```txt
{
  "semi": false,
  "singleQuote": true,
  "htmlWhitespaceSensitivity": "ignore",
  "trailingComma": "all",
  "arrowParens": "avoid"
}
```

### 创建dev文件

dev文件的作用是对项目进行打包的。首先创建文件夹scripts，并且在这个文件夹下面创建一个文件dev.js
(新增)code\scripts\dev.js

```js
console.log('正在打包开发环境...')
```

**测试**执行dev命令

```bash
$ pnpm run dev

> Vue3@1.0.0 dev C:\wuwuwu\Vue\Vue3
> node scripts/dev.js

正在打包开发环境...
```

### 当前项目目录

正常打印以后，表示当前是正常的了，之后我们对项目的打包配置就是在dev里面写的
当前项目文件目录就变成这样了

```
├─📁 packages/-------------- # 包
├─📁 scripts/--------------- # 配置文件夹
│ └─📄 dev.js--------------- # 打包配置
├─📄 .prettierrc------------ # 格式化配置文件
├─📄 package.json----------- # 项目的包
├─📄 pnpm-lock.yaml--------- # 下载包的详细信息
├─📄 pnpm-workspace.yaml---- # monorepo管理配置
└─📄 tsconfig.json---------- # ts配置
```

## 配置dev开发环境打包

如何配置？配置开发环境需要考虑哪些？
首先要知道，在进行打包的时候，需要打包出来什么样的文件，例如esm，cjs等，所以需要我们在执行打包的时候能提供不同的参数，打包出来不同的文件，例如：`node scripts/dev.js --format esm`生成的是esm类型的文件，`node scripts/dev.js --format cjs`生成的是cjs类型的文件

### 区分生成文件类型

code\package.json

```json
	...

  "scripts": {
    "dev": "node scripts/dev.js --format cjs"
  }

	...
```

想要拿到这些传过来的参数，可以用`process.arg`，拿到的格式是这样的

```bash
$ pnpm run dev

> Vue3@1.0.0 dev C:\wuwuwu\Vue\Vue3
> node scripts/dev.js --format cjs

[
  'C:\\nvm4w\\nodejs\\node.exe',
  'C:\\wuwuwu\\Vue\\Vue3\\scripts\\dev.js',
  '--format',
  'cjs'
]
```

可以看到拿到了`--format`和`cjs`，在数组里面。
但是我们想要的是对象，那么可以通过这种方式来获取

code\scripts\dev.js

```js
/**
 * 打包开发环境
 * 
 * node scripts/dev.js --format cjs
 */
import { parseArgs } from 'node:util'
const { values } = parseArgs({
  options: {
    format: {
      type: 'string', // 选项的类型
      short: 'f', // 选项的短名称
      default: 'esm', // 选项的默认值
    },
  },
})
console.log(values)
```

这样就可以拿到传递过来的参数，打印出来的结果是

```bash
$ pnpm run dev

> Vue3@1.0.0 dev C:\wuwuwu\Vue\Vue3
> node scripts/dev.js --format cjs

[Object: null prototype] { format: 'cjs' }
```

可以看到value就是一个对象，在format后面接的cjs就能拿到

### 区分打什么包（位置参数）

上面做完之后，我们就能正确解析出传过来的参数了，但是还有一个问题，就是monorepo管理的项目可能有很多包，比如reactivity包（创建一个文件夹叫reactivity），vue包（创建一个文件夹叫vue），打哪个包呢？所以还需要一个位置参数

code\package.json

```json
	...

"scripts": {
	"dev": "node scripts/dev.js reactivity --format cjs"
},

	...
```

这里传了reactivity，接着去拿这个参数
code\scripts\dev.js

```js
import { parseArgs } from 'node:util'

const {
  values: { format },
  positionals,
} = parseArgs({
  allowPositionals: true, // 允许位置参数

	...

})
console.log(format, positionals)
```

打印结果

```bash
$ npm run dev

> Vue3@1.0.0 dev
> node scripts/dev.js reactivity --format cjs

cjs [ 'reactivity' ]
```

这里加了`allowPositionals`参数，就能拿到我们传递的`reactivity`

接下来要拿到入口文件，上面写的reactivity实际上想做的是要打包reactivity这个包的文件，所以我们先要拿到目标的打包文件，对这个目标打包文件进行打包
code\scripts\dev.js

```js
	...

const {
  values: { format },
  positionals,
} = parseArgs({

	...

})

// 如果有获取到要打包的目标，否则默认打包整个 vue
const target = positionals.length ? positionals[0] : 'vue'
```

这样就可以拿到我们要打包的名字了，接着创建这些包。

### 创建待打包文件

因为不同的包，我们获取的方式是一样的，所以我们必须保证所有的包的文件层级，入口都是一样的，所以我们需要统一，这里创建了reactivity和vue的入口文件

```
├─📁 packages/-------------- # 所有的包
│ ├─📁 reactivity/---------- # 响应式包
│ │ └─📁 src/--------------- # 响应式核心代码
│ │   └─📄 index.ts--------- # 入口文件
│ └─📁 vue/----------------- # vue包
│   └─📁 src/--------------- # vue核心代码
│     └─📄 index.ts--------- # 入口文件
├─📁 scripts/--------------- # 打包配置
│ └─📄 dev.js--------------- # 开发环境配置文件
├─📄 .prettierrc------------ # 代码格式化配置
├─📄 package.json----------- # 项目管理
├─📄 pnpm-lock.yaml--------- # pnpm包管理
├─📄 pnpm-workspace.yaml---- # monorepo配置
└─📄 tsconfig.json---------- # ts配置
```

### 配置打包入口

code\scripts\dev.js

```js
import { resolve } from 'node:path'

...

const target = positionals.length ? positionals[0] : 'vue'
// 配置入口文件
const entry = resolve(__dirname, `../packages/${target}/src/index.ts`)
console.log('entry', entry)
```

这里跑一下的时候会发现报错，原因是`__dirname`是`commonJS`中的，`ESModule`是没有的，所有需要自己创建一个`__dirname`

```js
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { parseArgs } from 'node:util'

	...

const __filename = fileURLToPath(import.meta.url)
const __dirname = dirname(__filename);

// 如果有获取到要打包的目标，否则默认打包整个 vue
const target = positionals.length ? positionals[0] : 'vue'

// 配置入口文件
const entry = resolve(__dirname, `../packages/${target}/src/index.ts`)
console.log('entry', entry)
```

打印结果

```bash
$ npm run dev

> Vue3@1.0.0 dev
> node scripts/dev.js reactivity --format cjs

看看target reactivity E:\wuwuwu\vue3\vue3\code\scripts
entry E:\wuwuwu\vue3\vue3\code\packages\reactivity\src\index.ts
```

### 配置esbuild打包

知道要打什么包，并且拿到该包的路径，就可以进行esbuild配置了

```js
import esbuild from 'esbuild'

...

esbuild
  .context({
    entryPoints: [entry], // 入口文件
    outfile: resolve(
      __dirname,
      `../packages/${target}/dist/${target}.${format}.js`, // 针对不同的格式输出不同的文件
    ), // 输出文件
    format, // 输出格式
    platform: format === 'cjs' ? 'node' : 'browser', // 目标平台
    sourcemap: true, // 生成 source map
    bundle: true, // 打包所有依赖到一个依赖中
  })
  .then(ctx => {
    ctx.watch() // 监听文件变化，自动重新打包
  })
```

配置完了之后执行`pnpm run dev`

```bash
$ npm run dev

> Vue3@1.0.0 dev
> node scripts/dev.js reactivity --format cjs
```

服务启动中

### 测试配置

code\packages\reactivity\src\index.ts

```ts
export function foo(a, b) {
  return a + b
}
```

再看打包出来的文件
![[1-搭建环境-目录.png]]
打开这个文件观察一下还会发现里面有foo函数。

接着测试一下esm格式的文件
code\package.json

```json
{
	  ...

    "dev": "node scripts/dev.js reactivity --format esm"

	  ...

}
```

打包出来的文件就多了一些（自行测试）

### 下载vue

因为我们很多配置需要参考vue，所以我们先下载一个vue包

```bash
pnpm install vue
```

### IIFE打包

如果打包的是IIFE，这种IIFE需要对外提供一个全局变量，所以针对这个也需要处理
需要在reactivity文件夹下创建一个文件叫`package.json`的文件，并且在里面写配置，这里的配置参考的是vue的配置
在node_modules中找到reactivity，然后拿到它的package.json，进行参考
这里给出删减过后我们需要的
code\packages\reactivity\package.json

```json
{
  "name": "@vue/reactivity",
  "version": "1.0.0",
  "description": "响应式模块",
  "main": "dist/reactivity.cjs.js",
  "module": "dist/reactivity.esm.js",
  "files": ["index.js", "dist"],
  "sideEffects": false,
  "buildOptions": {
    "name": "VueReactivity",
    "formats": ["esm-bundler", "esm-browser", "cjs", "global"]
  }
}
```

> 注意：这里有一个配置叫`buildOptions`，它不是package的配置，是vue自己往里添加的，他的作用就是在使用cdn引入的时候，会往window里面加一些属性。
> 例如：使用window.vue.createApp，那么name就是vue，如果name是VueReactivity，那么就是window.VueReactivity。
> 这里的formats是生产环境用的，生产环境打出来的包就是这些了

导入reactivity的package.json，注意，因为require它是commonJS的，在ESModule中没有，和上面的`__dirname`一样，所以我们也需要重新获取require
code\scripts\dev.js

```js
	...
import { createRequire } from 'node:module'

	...

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
const require = createRequire(import.meta.url)

// 如果有获取到要打包的目标，否则默认打包整个 vue
const target = positionals.length ? positionals[0] : "vue";

// 配置入口文件
const entry = resolve(__dirname, `../packages/${target}/src/index.ts`);
const pkg = require(`../packages/${target}/package.json`)
console.log(pkg)

	...
```

这样的话，打印出来的pkg就是我们配置的package.json了

```bash
$ pnpm run dev

> Vue3@1.0.0 dev C:\wuwuwu\Vue\Vue3
> node scripts/dev.js reactivity --format esm

{
  name: '@vue/reactivity',
  version: '1.0.0',
  description: '响应式模块',
  main: 'index.js',
  module: 'dist/reactivity.esm.js.js',
  files: [ 'index.js', 'dist' ],
  sideEffects: false,
  buildOptions: {
    name: 'VueReactivity',
    formats: [ 'esm-bundler', 'esm-browser', 'cjs', 'global' ]
  }
}
```

之后就可以提供全局变量名了
code\scripts\dev.js

```js
...

esbuild
  .context({
    ...
    globalName: pkg.buildOptions?.name, // 全局变量名称，umd和iife格式需要
  })
  .then(ctx => {
    ctx.watch() // 监听文件变化，自动重新打包
  })
...

```

配置完以后，执行`"dev": "node scripts/dev.js reactivity --format iife"`可以看到我们打包出来的文件，这个文件被加载以后会暴露了一个变量出来，并且这个变量是通过var定义的，所以他会绑定到window上面了（这里不给出打包出来的代码，自行观察）

### vue包的package.json

code\packages\vue\src\package.json

```json
{
  "name": "vue",
  "version": "1.0.0",
  "description": "vue核心包",
  "main": "dist/vue.cjs.js",
  "module": "dist/vue.esm.js",
  "files": ["dist"],
  "sideEffects": false,
  "buildOptions": {
    "name": "Vue",
    "formats": ["esm-bundler", "esm-browser", "cjs", "global"]
  }
}
```

### shared包

创建code\packages\shared\src\index.ts
创建code\packages\shared\package.json

```json
{
  "name": "@vue/shared",
  "version": "1.0.0",
  "description": "工具函数",
  "main": "dist/shared.cjs.js",
  "module": "dist/shared.esm.js",
  "files": ["index.js", "dist"],
  "sideEffects": false,
  "buildOptions": {
    "name": "VueShared",
    "formats": ["esm-bundler", "esm-browser", "cjs", "global"]
  }
}
```

### monorepo使用依赖包

monorepo管理的项目获取依赖包和我们平常使用依赖包不同，例如：我们再shared/src/index.ts中写一个工具函数
code\packages\shared\src\index.ts

```ts
export function isObject(value: any): value is Record<any, any> {
  return value !== null && typeof value === 'object'
}
```

在这里export导出了一个函数叫isObject，我们希望在reactivity中使用
想在reactivity中使用，需要先下载这个依赖包

```bash
pnpm install @vue/shared --workspace --filter @vue/reactivity
```

这里的**workpace**表示使用monorepo中的包，**--filter**表示安装到@vue/reactivity这个项目中

安装完了以后就可以在reactivity项目的package.json中看到
code\packages\reactivity\package.json

```json
{
	...

  "dependencies": {
    "@vue/shared": "workspace:*"
  }
}
```

这样就安装成功了，接着测试一下
code\packages\reactivity\package.json

```json
import { isObject } from '@vue/shared'

isObject({})
```

但是这样写会报错
![[2-搭建环境-报错.png]]

这里是ts找不到，所以需要配置一下
code\tsconfig.json

```json
{
  "compilerOptions": {

	  ...

    "paths": {
      "@vue/*": ["packages/*/src"]
    }
  }
}

```

表示我们再使用@vue/下所有的引用的时候，就去packages文件夹下去找，也就不会报错了
接着执行`pnpm run dev`就能正常了

## 当前项目目录

```
├─📁 packages/----------------------- # 所有的包
│ ├─📁 reactivity/------------------- # 响应式包
│ │ ├─📁 dist/----------------------- # 打包出来的文件
│ │ │ ├─📄 reactivity.cjs.js--------- # cjs格式
│ │ │ ├─📄 reactivity.cjs.js.map----- # 打包出来的souce map
│ │ │ ├─📄 reactivity.esm.js--------- # 打包出来的esm
│ │ │ ├─📄 reactivity.esm.js.map----- # 打包出来的souce map
│ │ │ ├─📄 reactivity.iife.js-------- # iife格式
│ │ │ └─📄 reactivity.iife.js.map---- # 打包出来的souce map
│ │ ├─📁 node_modules/--------------- # reactivity的依赖
│ │ ├─📁 src/------------------------ # 响应式核心代码
│ │ │ └─📄 index.ts------------------ # 入口文件
│ │ └─📄 package.json---------------- # reactivity依赖的包管理
│ ├─📁 shared/----------------------- # 工具函数
│ │ ├─📁 src/------------------------ # 工具函数核心代码
│ │ │ └─📄 index.ts------------------ # 入口文件
│ │ └─📄 package.json---------------- # package文件
│ └─📁 vue/-------------------------- # vue包
│   ├─📁 src/------------------------ # vue核心代码
│   │ └─📄 index.ts------------------ # 入口文件
│   └─📄 package.json---------------- # package文件
├─📁 scripts/------------------------ # 打包配置
│ └─📄 dev.js------------------------ # 开发环境配置文件
├─📄 .prettierrc--------------------- # 代码格式化配置
├─📄 package.json-------------------- # 项目管理
├─📄 pnpm-lock.yaml------------------ # pnpm包管理
├─📄 pnpm-workspace.yaml------------- # monorepo配置
└─📄 tsconfig.json------------------- # ts配置
```

## 开发环境配置完整代码

code\scripts\dev.js

```js
/**
 * 打包开发环境
 * 
 * node scripts/dev.js --format cjs
 */
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { parseArgs } from 'node:util'
import esbuild from 'esbuild'
import { createRequire } from 'node:module'

/**
 * 解析命令行参数
 */
const {
  values: { format },
  positionals,
} = parseArgs({
  allowPositionals: true, // 允许位置参数
  options: {
    format: {
      type: 'string', // 选项的类型
      short: 'f', // 选项的短名称
      default: 'esm', // 选项的默认值
    },
  },
})

// 创建esm的__filename和__dirname
const __filename = fileURLToPath(import.meta.url)
const require = createRequire(import.meta.url)
const __dirname = dirname(__filename)

// 如果有获取到要打包的目标，否则默认打包整个 vue
const target = positionals.length ? positionals[0] : 'vue'

// 配置入口文件
const entry = resolve(__dirname, `../packages/${target}/src/index.ts`)

// 导入package.json
const pkg = require(`../packages/${target}/package.json`)

esbuild
  .context({
    entryPoints: [entry], // 入口文件
    outfile: resolve(
      __dirname,
      `../packages/${target}/dist/${target}.${format}.js`, // 针对不同的格式输出不同的文件
    ), // 输出文件
    format, // 输出格式
    platform: format === 'cjs' ? 'node' : 'browser', // 目标平台
    sourcemap: true, // 生成 source map
    bundle: true, // 打包所有依赖到一个依赖中
    globalName: pkg.buildOptions?.name, // 全局变量名称，umd和iife格式需要
  })
  .then(ctx => {
    ctx.watch() // 监听文件变化，自动重新打包
  })
```
