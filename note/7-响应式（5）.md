前面讲了 ref 来实现响应式，接下来讲讲 reactive 来实现响应式

## demo 文件

packages\reactivity\examples\demo.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const state = reactive({
        a: 0,
      })
      let e = effect(() => {
        console.log(state.a)
      })

      setTimeout(() => {
        state.a = 1
      }, 1000)
    </script>
  </body>
</html>
```

这里使用 reactive，接下来写 reactive 函数

## reactive 文件

packages\reactivity\src\reactive.ts

```ts
import { isObject } from '@vue/shared'

export function reactive(target) {
  /**
   * 为什么这里返回一个函数呢？
   * 因为其他地方需要调用这个函数来创建响应式对象，比如shallowReactive
   */
  return createReactiveObject(target)
}

function createReactiveObject(target) {
  // reactive必须接收一个对象
  if (!isObject(target)) {
    // 如果target不是对象，直接返回target
    return target
  }

  return new Proxy(target, {
    get(target, key, receiver) {
      /**
       * target = { a : 0 }
       * 收集依赖，绑定target中某一个 key 和 sub 之间的关系
       */
      return Reflect.get(target, key) // TODO receiver先不写，等会再来填这个坑
    },

    set(target, key, newVal, receiver) {
      // 触发更新，set的时候，通知之前收集的依赖，重新执行
      return Reflect.set(target, key, newVal)
    },
  })
}
```

reactive 只接受对象，并且通过 Proxy 来监听对象的使用和修改

packages\reactivity\src\index.ts

```ts
	...

export * from './reactive'
```

上面的 reactive 通过 Proxy 来监听对象，所以在 get 中需要收集依赖，在 set 的时候还需要触发更新
收集依赖的思路和 ref 中的 trackRef 差不多，但是 track 需要接收一个 dep。
![[7-响应式（5）-ACC03056D9868467D468C1C970FD4516.png]]
这里就是 RefImpl 类中的 get 中使用 trackRef 的方式
可以看到 trackRef 接受的是 dep，实际上就是 RefImpl 的实例，也就是 ref
既然我们想使用 trackRef，那么也需要传入 dep，但是 dep 的主要属性就是 subs、subsTail，那么我们传入的对象有这两个对象也是可以的
所以我们可以在 reactive 中创建一个对象，把这个对象加上这两个属性

## reactive 收集依赖

packages\reactivity\src\reactive.ts

```ts

	...

	class Dep {

	  // 订阅者链表的头节点
	  subs: Link

	  // 订阅者链表的尾节点
	  subsTail: Link

	  constructor() {}

	}
```

然后我们在收集依赖的时候使用 trackRef

```ts

	...

/**
 * 绑定 target 的 key 关联的所有的 dep
 * obj = { a: 0 }
 * targetMap = {
 *  [obj]: {
 *    a: Dep,
 *    b: Dep
 *  }
 * }
 */
const targetMap = new WeakMap()

function track(target, key) {
  // 判断是否在执行effect
  if (!activeSub) {
    return
  }

  /**
   * 找 depsMap = {
   *  a: Dep,
   *  b: Dep
   * }
   */
  let depsMap = targetMap.get(target)
  if (!depsMap) {
    /**
     * 没有depsMap,就是之前没有收集过这个对象的任何key做关联关系
     * 那就创建一个新的 depsMap 来保存 target 和 depsMap 之间的关联关系
     */
    depsMap = new Map()
    targetMap.set(target, depsMap)
  }

  let dep = depsMap.get(key)
  if (!dep) {
    /**
     * 第一次找key关联的dep对象是找不到的，需要先创建出来并且保存到depsMap中
     */
    dep = new Dep()
    depsMap.set(key, dep)
  }

  /**
   * 这里已经可以拿到dep了，我们就可以把dep传递给link，让link
   * 帮我们创建出节点
   */
  link(dep, activeSub)
}

// 创建一个类似RefImpl的dep类
class Dep {
  // 订阅者链表的头节点
  subs: Link

  // 订阅者链表的尾节点
  subsTail: Link
  constructor() {}
}

```

reactive 保存依赖的思路：[[7-响应式（5）-FCBFB6BAAEA0BEE5B75C9E82B0B72475.excalidraw]]

收集依赖的 track 函数，已经开发完了，接下来可以在 get 中收集依赖了

```ts
function createReactiveObject(target) {
    ...
  return new Proxy(target, {
    get(target, key, receiver) {
      /**
       * target = {a: 0}
       * 收集依赖，绑定target中某一个key和sub之间的关系
       */
      console.log('get', key, target)
      track(target, key) // 收集依赖

      // TODO receiver先不写，等会再来填这个坑
      return Reflect.get(target, key)
    },
      ...
  })
}
```

这样的话，依赖就被收集到了
![[7-响应式（5）-6B3588A332F1FC38AB2F2F28FA907DCD.png]]
这里就是收集到的依赖

## reactive 触发更新

现在处理更新数据，触发逻辑
packages\reactivity\src\reactive.ts

```ts
...

function trigger(target, key) {
  /**
   * 找depsMap = {
   *  a: Dep,
   *  b: Dep
   * }
   */
  const depsMap = targetMap.get(target)

  if (!depsMap) {
    /**
     * depsMap 没有，表示这个对象，从来没有任何属性在sub中访问过
     */
    return
  }

  /**
   * 找到 key 对应的Dep
   * key => Dep
   */
  const dep = depsMap.get(key)
  if (!dep) {
    // dep 不存在，表示这个key没有在sub中访问过
    return
  }

  /**
   * 找到 dep 的 subs 通知他们重新执行
   */
  propagate(dep.subs)
}

...
```

packages\reactivity\src\reactive.ts

```ts
	...

/**
 * 触发对象更新
 * @param target 接受一个对象
 * @param key 监听变化的对象的key
 * @returns void
 */
function trigger(target, key) {
  /**
   * 找 depsMap = {
   *  a: Dep,
   *  b: Dep
   * }
   */

  const depsMap = targetMap.get(target)

  if (!depsMap) {
    // depsMap 没有，表示这个对象从来没有任何属性在 sub 中访问过
    return
  }

  /**
   * 找到 key 对应的Dep
   * key => Dep
   */
  const dep = depsMap.get(key)
  if (!dep) {
    // dep不存在，表示这个key没有在sub中访问过
    return
  }

  // 找到 dep 的 subs 通知它们更新
  propagate(dep.subs)
}
```

这个方法是在依赖变化的时候，触发更新

```ts
function createReactiveObject(target) {

    ...

  return new Proxy(target, {

      ...

    set(target, key, newValue, receiver) {
      /**
       * 触发更新，set的时候，通知之前收集的依赖，重新执行
       */
      const res = Reflect.set(target, key, newValue)
      trigger(target, key)

      return res
    },
  })
}
```

这样就能在更新的时候，去触发依赖的更新。

## receiver

接下来填一个坑，看一下 receiver 是做什么用的

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const state = reactive({
        a: 0,
        get count() {
          return this.a
        },
      })
      let e = effect(() => {
        console.log(state.count)
      })

      setTimeout(() => {
        state.a = 1
      }, 1000)
    </script>
  </body>
</html>
```

这里在一秒钟之后没有任何打印，他们的关系是这样的：

```plain
setTimeout 中修改了 state.a
count中依赖了a
effect中依赖了count
```

在官方中是会在一秒钟打印 1 的。
所以，在官方中，count 里面的 a 也是要收集的，那么怎么收集呢？

packages\reactivity\src\reactive.ts

```ts

function createReactiveObject(target) {

    ...

  return new Proxy(target, {
    get(target, key, receiver) {

        ...

      return Reflect.get(target, key, receiver)
    },
    set(target, key, newValue, receiver) {
      /**
       * 触发更新，set的时候，通知之前收集的依赖，重新执行
       */
      const res = Reflect.set(target, key, newValue, receiver)
      trigger(target, key)

      return res
    },
  })
}
```

很简单，只需要加上 receiver，来保证 get 中的 this 指向的是代理对象 proxy，在收集依赖的时候就能正常收集到 a 了，这里在 ge 和 set 中都加上了 receiver

## 场景一：

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const obj = {
        a: 0,
      }

      const state1 = reactive(obj)
      const state2 = reactive(obj)
      console.log('state === state2', state1 === state2) // false
    </script>
  </body>
</html>
```

在这里打印的 state === state2，就是 false，但是你如果看官方的话，他就是 true，为什么呢？这里就是一个 obj 对象，被多次代理了，所以现在要解决的问题是防止一个对象被重复代理，如果一个对象被重复代理了，那我就返回之前的那个对象。所以：

### 创建一个全局变量

packages\reactivity\src\reactive.ts

```ts
export function reactive(target) {
  // 为什么这里返回一个函数呢？因为其他地方需要调用这个函数来创建响应式对象，比如shallowReactive
  return createReactiveObject(target)
}

/**
 * 保存 target 和响应式对象之间的关联关系
 * target => proxy
 */
const reactiveMap = new WeakMap()

	...
```

然后每当返回一个 proxy，都给他保存起来
packages\reactivity\src\reactive.ts

```ts
function createReactiveObject(target) {

    ...

  const proxy = new Proxy(target, {

      ...

  })

  reactiveMap.set(target, proxy)

  return proxy
}
```

然后再创建 proxy 之前，先进行判断

```ts
function createReactiveObject(target) {

    ...

   /**
   * 获取到之前这个 target 创建的代理对象
   */
  const existingProxy = reactiveMap.get(target)

  // 如果这个target之前创建过响应式对象，那么就直接返回这个响应式对象
  if (existingProxy) {
    return existingProxy
  }

    ...

}
```

当这样处理之后，如果该对象之前保存过代理对象，那么该对象直接返回之前创建的代理对象，前面的判断的是否相等就是 true 了

## 代码抽取

packages\reactivity\src\reactive.ts

```ts
import { isObject } from '@vue/shared'
import { Link, link, propagate } from './system'
import { activeSub } from './effect'

export function reactive(target) {
  // 为什么这里返回一个函数呢？因为其他地方需要调用这个函数来创建响应式对象，比如shallowReactive
  return createReactiveObject(target)
}
// 只需要创建一次这个对象就行了
const mutableHandlers = {
  get(target, key, receiver) {
    /**
     * target = {a: 0}
     * 收集依赖，绑定target中某一个key和sub之间的关系
     */
    track(target, key)

    // receiver是用来改变访问器中的this访问对象
    return Reflect.get(target, key, receiver)
  },
  set(target, key, newValue, receiver) {
    /**
     * 触发更新，set的时候，通知之前收集的依赖，重新执行
     */
    const res = Reflect.set(target, key, newValue, receiver)
    trigger(target, key)

    return res
  },
}

...



function createReactiveObject(target) {
    ...

  const proxy = new Proxy(target, mutableHandlers)

  	...

}

	  ...
```

把 proxy 的 handler 抽出来放到 mutableHandlers 中，再把 mutalbeHandlers 放到 proxy 中

## 新增工具函数

packages\shared\src\index.ts

```ts
...

/**
 * 判断值有没有发生过变化，如果发生过变化就返回true
 * @param newValue 新的值
 * @param oldValue 旧的值
 * @returns boolean
 */
export function hasChanged(newValue, oldValue) {
  return !Object.is(newValue, oldValue)
}
```

该工具函数主要判断两个值是否有变化

## 场景二：

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const obj = {
        a: 0,
      }

      const state1 = reactive(obj)
      const state2 = reactive(state1)
    </script>
  </body>
</html>
```

能用响应式对象再创建一个响应式对象吗
当前，肯定不是同一个响应式对象，state 和 state2 不相等
在官方中，是相等的，也就意味着，不能把代理过的响应式对象，传进来再代理一次，如果再传，那么就返回，不处理该对象

```ts

	...

/**
 * 保存所有使用 reactive 创建出来的响应式对象
 */
const reactiveSet = new WeakSet()

function createReactiveObject(target) {

	...

  /**
   * 判断这个target在不在reactiveSet中，如果在，表示他已经是一个代理对象了，直接返回
   */
  if (isReactive(target)) {
    return target
  }

  /**
   * 获取到之前这个 target 创建的代理对象
   */
  const existingProxy = reactiveMap.get(target)

  // 如果这个target之前创建过响应式对象，那么就直接返回这个响应式对象
  if (existingProxy) {
    return existingProxy
  }

  const proxy = new Proxy(target, mutableHandlers)
 
  reactiveMap.set(target, proxy)
 
  /**
   * 保存响应式对象到reactiveSet种
   */
  reactiveSet.add(proxy)
  return proxy
}

...

/**
 * 判断 target 是不是响应式对象，只要在 reactiveSet 中,他就是响应式对象
 * @param target 判断是不是响应式的对象
 * @returns
 */
export function isReactive(target) {
  return reactiveSet.has(target)
}
```

    这里的思路其实和场景一差不多，都是创建一个全局变量，把 proxy 保存下来，然后对传入的对象做一个判断，并且这里新增了一个工具函数，isReactive

## 场景三：

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const obj = {
        a: 0,
      }

      const state = reactive(obj)
      let e = effect(() => {
        console.log(state.a)
      })

      setTimeout(() => {
        state.a = 0
      }, 1000)
    </script>
  </body>
</html>
```

在上面的代码中，一秒钟后，effect 会重新执行，打印 0，这个是我们的代码会这样，但是在官方的代码中，不会这样，而是会给 a 设置相同的值的时候，不进行更新依赖

### reactive 中判断新老变化

packages\reactivity\src\reactive.ts

```ts
	...

// 只需要创建一次这个对象就行了
const mutableHandlers = {

    ...

  set(target, key, newValue, receiver) {
    const oldValue = target[key]

    /**
     * 触发更新，set的时候，通知之前收集的依赖，重新执行
     */
    const res = Reflect.set(target, key, newValue, receiver)
    if (hasChanged(newValue, oldValue)) {
      trigger(target, key)
    }

    return res
  },
}

	...

```

### ref 中判断新老变化

packages\reactivity\src\ref.ts

```ts
import { reactive } from './reactive'

	...

/**
 * ref的类
 */
class RefImpl {
    ...

  set value(newValue) {
      // 判断是否需要更新
    if (hasChanged(newValue, this._value)) {
      // 触发更新
      this._value = isObject(newValue) ? reactive(newValue) : newValue
      triggerRef(this)
    }
  }
}
	...
```

这样增加判断之后，如果修改的是相同的值就不会重新更新 effect 了

## 场景四

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div id="app"></div>
    s
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const a = ref(0)
      const state = reactive({
        a,
      })

      let e = effect(() => {
        // 不用 state.a.value
        console.log(state.a)
      })

      setTimeout(() => {
        // 更新的时候也不用加 .value
        state.a = 1
      }, 1000)
    </script>
  </body>
</html>
```

简单来说，就是把一个 ref 传入到 reactive 中的时候，用 reactive 返回的变量访问 ref 的值，或者设置 ref 的值的时候不加 value
在官方的 reactive 中，如果把一个 ref 传入到 reactive 中，在获取的时候和修改的时候都不需要加 .value
ref 和 reactive 在一起时，他们的结构是这样的：[[reactive和ref在一起]]

### reactive 中的解包

packages\reactivity\src\reactive.ts

```ts
import { isRef } from './ref'

	...

// 只需要创建一次这个对象就行了
const mutableHandlers = {
  get(target, key, receiver) {
    /**
     * target = {a: 0}
     * 收集依赖，绑定target中某一个key和sub之间的关系
     */
    track(target, key)

    const res = Reflect.get(target, key, receiver)
    if (isRef(res)) {
      // 如果 target.a 是一个ref，那么就直接把值给他，不要让他 .value
      return res.value
    }
    // receiver是用来改变访问器中的this访问对象
    return res
  },

    ...

}
```

这样之后，在获取的时候就能直接解包了

## 场景五：

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      const a = ref(0)
      const state = reactive({
        a,
      })

      let e = effect(() => {
        // 不用 state.a.value
        console.log(state.a)
      })

      setTimeout(() => {
        // 更新的时候也不用加 .value
        state.a = 1
        console.log('state中的a', state.a) // 1
        console.log('a本身', a.value) // 0
      }, 1000)
    </script>
  </body>
</html>
```

上述的问题是，state 中的 a 和 a 本身不一致了，明明把 a 传入 reactive 中了，reactive 返回的 state 修改之后，a 本身没有跟着修改，为什么呢？
原因很简单，因为上面在修改 state.a 的时候，直接把 state 中中的 a 修改了，但是没有处理 a 作为 ref 时的值

### 修改 ref 的值

packages\reactivity\src\reactive.ts

```ts

// 只需要创建一次这个对象就行了
const mutableHandlers = {

    ...


  set(target, key, newValue, receiver) {
    const oldValue = target[key]

    /**
     * 触发更新，set的时候，通知之前收集的依赖，重新执行
     */
    const res = Reflect.set(target, key, newValue, receiver)

    /**
     * 如果 target.a 是一个ref，那么就直接把值给他，不要让他 .value
     * 如果更新了 state.a 它之前是一个 ref, 那么会修改原始的 ref.value 的值 等于 newValue
     * 如果 newValue 是一个ref，那就算了 （ state.a=ref(1) ）
     */
    if (isRef(oldValue) && !isRef(newValue)) {
      /**
       * const a = ref(0)
       * target = {a: a}
       * 更新target.a = 1, 它就等于更新了 a.value
       * a.value = 1
       */
      oldValue.value = newValue
      return res // 这里不用走下面的trigger，因为oldValue是一个ref，它发生了修改会自动触发ref中的set
    }

    /**
     * 如果新值和老值不一样，触发更新
     * 先 set 在通知更新（return）
     */
    if (hasChanged(newValue, oldValue)) {
      trigger(target, key)
    }

    return res
  },
}
```

在这里，如果上一次的值是 ref，这一次的值不是 ref，那么要把这一次的值赋值给上一次的 ref，其实也就是上面案例的 a，这样不光 reactive 中的 a 变化了，ref 中的 a 也变化了，并且会对应的触发 ref 中 a 的依赖和 reactive 的依赖

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        padding: 150px;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <button id="flagBtn">update flag</button>
    <button id="nameBtn">update name</button>
    <button id="ageBtn">update age</button>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const a = ref(0)
      const state = reactive({
        a,
      })

      let e = effect(() => {
        // 不用 state.a.value
        console.log(state.a)
      })

      let e1 = effect(() => {
        console.log(a.value)
      })

      setTimeout(() => {
        // 更新的时候也不用加 .value
        state.a = 1
      }, 1000)
    </script>
  </body>
</html>
```

在这个案例中，两个 effect 都会触发，修改 state.a 的话，a.value 也会更新

## 代码重构

packages\reactivity\src\dep.ts

```ts
import { activeSub } from './effect'
import { link, propagate, Link } from './system'

/**
 * 绑定 target 的 key 关联的所有的 dep
 * obj = { a: 0 }
 * targetMap = {
 *  [obj]: {
 *    a: Dep,
 *    b: Dep
 *  }
 * }
 */
const targetMap = new WeakMap()

export function track(target, key) {
  if (!activeSub) {
    return
  }

  /**
   * 找depsMap = {
   *  a: Dep,
   *  b: Dep
   * }
   */
  let depsMap = targetMap.get(target)
  if (!depsMap) {
    /**
     * 没有depsMap, 就是之前没有收集过这个对象的任何key做关联关系
     * 那就创建一个新的，保存target和depsMap之间的关联关系
     */
    depsMap = new Map()
    targetMap.set(target, depsMap)
  }

  let dep = depsMap.get(key)
  if (!dep) {
    /**
     * 第一次手机这个对象，没找到，创建一个新的，并且保存到depsMap中
     */
    dep = new Dep()
    depsMap.set(key, dep)
  }

  /**
   * 绑定 dep 和 sub 之间的关联关系
   */
  link(dep, activeSub)
}

export function trigger(target, key) {
  /**
   * 找depsMap = {
   *  a: Dep,
   *  b: Dep
   * }
   */
  const depsMap = targetMap.get(target)

  if (!depsMap) {
    /**
     * depsMap 没有，表示这个对象，从来没有任何属性在sub中访问过
     */
    return
  }

  /**
   * 找到 key 对应的Dep
   * key =》 Dep
   */
  const dep = depsMap.get(key)
  if (!dep) {
    // dep 不存在，表示这个key没有在sub中访问过
    return
  }

  /**
   * 找到 dep 的 subs 通知他们重新执行
   */
  propagate(dep.subs)
}

class Dep {
  /**
   * 订阅者链表的头节点
   */
  subs: Link

  /**
   * 订阅者链表的尾节点
   */
  subsTail: Link
  constructor() {}
}
```

packages\reactivity\src\baseHandlers.ts

```ts
import { hasChanged } from '@vue/shared'
import { isRef } from 'vue'
import { track, trigger } from './dep'

// 只需要创建一次这个对象就行了
export const mutableHandlers = {
  get(target, key, receiver) {
    /**
     * target = {a: 0}
     * 收集依赖，绑定target中某一个key和sub之间的关系
     */
    track(target, key)

    const res = Reflect.get(target, key, receiver)
    if (isRef(res)) {
      // 如果 target.a 是一个ref，那么就直接把值给他，不要让他 .value
      return res.value
    }
    // receiver是用来改变访问器中的this访问对象
    return res
  },

  set(target, key, newValue, receiver) {
    const oldValue = target[key]

    /**
     * 触发更新，set的时候，通知之前收集的依赖，重新执行
     */
    const res = Reflect.set(target, key, newValue, receiver)

    /**
     * 如果 target.a 是一个ref，那么就直接把值给他，不要让他 .value
     * 如果更新了 state.a 它之前是一个 ref, 那么会修改原始的 ref.value 的值 等于 newValue
     * 如果 newValue 是一个ref，那就算了 （ state.a=ref(1) ）
     */
    if (isRef(oldValue) && !isRef(newValue)) {
      /**
       * const a = ref(0)
       * target = {a: a}
       * 更新target.a = 1, 它就等于更新了 a.value
       * a.value = 1
       */
      oldValue.value = newValue
      return res // 这里不用走下面的trigger，因为oldValue是一个ref，它发生了修改会自动触发ref中的set
    }

    /**
     * 如果新值和老值不一样，触发更新
     * 先 set 在通知更新（return）
     */
    if (hasChanged(newValue, oldValue)) {
      trigger(target, key)
    }

    return res
  },
}
```

## 场景六：

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <script type="module">
      // 我们自己的
      import { ref, effect, reactive } from '../dist/reactivity.esm.js'

      // 官方的
      // import {
      //   ref,
      //   effect,
      // } from '../../../node_modules/vue/dist/vue.esm-browser.prod.js'

      const a = ref(0)
      const state = reactive({
        a: {
          b: 0,
        },
      })

      let e = effect(() => {
        console.log(state.a.b)
      })

      setTimeout(() => {
        state.a.b = 1
      }, 1000)
    </script>
  </body>
</html>
```

在这个案例中，state.a.b = 1 执行完以后，effect 不会执行
因为 a 这个对象，他不是响应式，外层 reactive 包裹的对象，但是 a 这个对象里面的内容不是响应式

packages\reactivity\src\baseHandlers.ts

```ts
// 只需要创建一次这个对象就行了
export const mutableHandlers = {

    ...

  get(target, key, receiver) {

    /**
     * target = {a: 0}
     * 收集依赖，绑定target中某一个key和sub之间的关系
     */
    track(target, key)

    const res = Reflect.get(target, key, receiver)

    if (isRef(res)) {
      // 如果 target.a 是一个ref，那么就直接把值给他，不要让他 .value
      return res.value
    }

    // 如果获取到的是一个对象，那么把这个对象进行reactive包裹，并返回
    if (isObject(res)) {
      return reactive(res)
    }
    // receiver是用来改变访问器中的this访问对象
    return res
  },
}
```

如果检测到是一个对象，那么把这个对象转成响应式对象
